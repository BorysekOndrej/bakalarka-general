version: '3.5'

services:

  flask:
    ports:
      - "23:22"

  sqlite-web:
    container_name: sqlite-web
    image: coleifer/sqlite-web
    volumes:
      - db:/data
    environment:
      - SQLITE_DATABASE=test.db
    ports:
      - "5001:8080"
